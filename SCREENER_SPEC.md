# 되돌림(눌림목) 스크리너 — 불변 스펙

이 문서는 **요구사항 명세이자 회귀 방지 계약**이다.
코드를 수정할 때마다 §1의 INV 항목이 여전히 성립하는지 확인하고, §7 검증을 실행해
**결과를 숫자로 보고**하라. "고쳤습니다"만 쓰고 끝내지 말 것.

이 문서와 코드가 충돌하면 **이 문서가 우선**한다.
스펙 자체를 바꿔야 한다고 판단되면, 코드를 고치기 전에 먼저 물어라.

주 구현 파일: `scripts/kr_pullback_screener.py`
관련 파일: `scripts/daily_screener.py`, `scripts/pullback_backtest.py`, `dashboard/index.html`

---

## 0. 범위

- 한국 주식(KRX) 일봉 기준, 장 마감 후 하루 1회 실행하는 후보 종목 스크리너
- **롱 관점만.** 역배열(하락 추세) 되돌림은 만들지 않는다
- 매매 신호가 아니라 관찰 후보 리스트를 뽑는 도구
- 유니버스는 시총 하한 때문에 200~250종목 수준으로 작다는 것을 전제로 설계한다

---

## 1. 절대 불변 규칙 (INV)

위반하는 코드는 병합하지 않는다.

### INV-1. 종가 전용
되돌림 판정과 추세 판정에 쓰이는 **모든** 값은 종가(`close`)로만 계산한다.
장중 고가(`high`)·저가(`low`)는 신호 계산에 **일절 쓰지 않는다.**

- 현재 `load_history()`는 `high`/`low` 컬럼을 살려두고 있다. 양수 검증(sanity check)에만
  쓰고 있으므로 동작은 맞지만, **검증 직후 두 컬럼을 drop** 해서 하류 코드가
  구조적으로 접근할 수 없게 만들 것. 주석만으로는 부족하다
- 이유: 장중 저가로만 20일선을 찍고 종가는 위에서 끝난 케이스를 잡으면 안 된다

### INV-2. 당일 제외
20일 고점 H와 저점 L은 **기준일(당일)을 제외한** 직전 20 거래일에서 구한다.

- 현재 구현(`prior_close = close.shift(1)` → `rolling(w).max()/.min()`)이 정확하다. **유지할 것**
- 당일 종가가 H나 L에 포함되면 되돌림비율이 항상 0 또는 1로 붙어버린다

### INV-3. 되돌림비율은 절대 삭제하지 않는다
```
retrace_ratio = (H − 당일 종가) / (H − L)
```
이 값은 **밴드를 벗어난 종목이라도** 결과에 컬럼으로 남긴다.

- 현재 `passes_retrace_band` 불리언 플래그와 `retrace_ratio` 값을 분리해 둔 설계가 정답이다. **유지할 것**
- 필터로 잘라내면서 컬럼을 없애는 변경은 금지

### INV-4. 고점 경과일 하한 ✅ 2026-09-06 반영
`days_since_high20`이 **2 미만이면 무조건 탈락**시킨다. 상한도 둔다(기본 15).

- 구현: `CONFIG["DAYS_SINCE_HIGH_MIN"]=2`, `CONFIG["DAYS_SINCE_HIGH_MAX"]=15`.
  `screen_on_date()`와 `dashboard_refresh_kr.py::row_for_dashboard()` 양쪽에 동일 적용
- 탈락 종목도 행은 남기고 `is_candidate=False` + `exclude_reason`으로 사유를 남긴다(INV-3 유지)
- 회귀 테스트: `tests/test_screener.py` V-3a(합성)·V-3b(R-1)·V-3c(전 종목 불변식)

- 이유: 고점 다음날은 되돌림 구간이 형성되지 않았다. 그냥 음봉 하나다
- 이유: 되돌림 구간이 1일이면 `vol_dryup_ratio`가 "1일 평균 ÷ 상승구간 평균"이 되어
  통계적으로 무의미하다
- **필터에 넣어야 한다.** 점수 가중치로만 반영하는 것으로는 부족하다

### INV-5. 설정 단일 출처
모든 임계값은 `CONFIG` dict **한 곳에만** 존재한다.
함수 기본 인자·매직 넘버·하드코딩 상수로 임계값이 새어 나가면 안 된다.
각 항목에는 "올리면/내리면 어떻게 되는지" 한국어 주석을 붙인다.
(현재 CONFIG 주석 스타일이 좋다. 새 항목도 같은 형식으로 추가할 것)

### INV-6. 수정주가 ✅ 2026-09-06 검증 완료 (전제 정정)
가격은 **수정주가(액면분할·유무상증자 반영)** 를 써야 한다.

**검증 결과: 현재 데이터 소스(네이버)는 이 조건을 이미 충족한다.**
이전 판(`Prices are NOT split-adjusted`)은 검증 없이 적힌 서술이었고 사실이 아니었다.

- 근거 1: 유니버스 141종목 × 3년 전 구간에서 종가 대비 종가 비율이 KRX 가격제한폭(±30%)
  밖으로 벗어난 사례 **0건**. 미조정 분할이면 0.5·0.2·0.1 같은 값이 반드시 남는다
  (검출된 17건은 전부 비율 0.700 = 하한가 정상 거래일)
- 근거 2: 네이버 종가 ÷ 야후 raw(=분할 조정 시리즈) 중앙값 **1.0000**
  (005930·051910·035420). 야후 adjclose 대비로는 1~3% 높고, 그 차이가 배당분이다
- **배당은 미반영.** 기술적 되돌림 판정에는 배당 조정이 필요하지 않으므로 허용한다
- 상시 가드: `detect_price_discontinuity()`가 매 실행마다 ±30% 밖 종가 점프를 검사하고
  해당 종목을 제외 + 로그. 임계값은 `CONFIG["PRICE_GAP_MAX_RATIO"/"PRICE_GAP_MIN_RATIO"]`
  (주석이 아니라 필터로 강제한다)

---

## 2. 되돌림 정의

기준일 t, 룩백 N(`RETRACE_WINDOW`, 기본 20)에 대해:

```
window   = close[t−N : t]          # ★ 당일 t 제외
H        = max(window)
L        = min(window)
H_date   = argmax(window)
retrace_ratio    = (H − close[t]) / (H − L)
days_since_high  = t − H_date       (거래일 수)
dd_from_high     = (H − close[t]) / H      # ← 추가 필요
range_pct        = (H − L) / L             # ← 추가 필요
```

### 알려진 한계 (반드시 인지할 것)
L을 20일 창 전체에서 찾으면 **신선한 고점과 낡은 저점이 섞인다.**
그 결과 `retrace_ratio`는 "직전 상승 스윙 대비 얼마나 눌렸나"가 아니라
"20일 박스 안 어디쯤 있나"를 재게 되고, 박스권 종목이 섞여 들어온다.

따라서 `dd_from_high`(고점 대비 낙폭)를 **항상 함께 출력**하고 상한 필터를 둔다.

개선안(선택): L을 "H를 만든 상승 다리 구간"에서만 찾는 `retrace_leg` 컬럼을 추가해
기존 `retrace_ratio`와 **나란히 병기**한다. 기존 컬럼을 대체하지 말 것.

---

## 3. 필터 순서 (퍼널)

이 순서로 적용하고, **각 단계 통과 종목 수를 퍼널로 기록·출력**한다.
어느 조건이 몇 종목을 잘랐는지 보이지 않으면 튜닝이 불가능하다.
현재는 최종 통과 수만 찍고 있어 어떤 조건이 병목인지 알 수 없다.

1. **유니버스**
   - 시가총액 하한 (`MCAP_MIN_EOK`, 기본 15,000억 = 1.5조)
   - 우선주 — 종목명 패턴 + **종목코드 끝자리가 0이 아닌 경우**도 함께 볼 것
   - 스팩(`스팩`, `기업인수목적`), 리츠 제외
     (현재 `리츠$` 끝 앵커 처리는 `메리츠금융지주` 오탐 방지로 올바르다. 유지)
   - **ETF/ETN/ELW 제외 — 현재 누락.** `KODEX`, `TIGER`, `KBSTAR`, `ACE`, `RISE`,
     `PLUS`, `SOL`, `KOSEF`, `ARIRANG`, `HANARO` 등 브랜드 접두어로 거른다
   - 상장 60일 미만 제외
   - 20일 평균 거래대금 하한 (느슨한 기본값)
   - **관리종목/거래정지/투자경고 제외 — 현재 누락** (§5 참조)

2. **추세 (정배열)**
   - MA5 > MA20 > MA60 > MA120
     (되돌림 중에는 MA5가 MA20 아래로 잠깐 내려가는 게 정상이므로
      MA5 vs MA20 에만 완화 계수를 허용해도 된다)
   - 60일선 기울기 양수.
     **`MA60_SLOPE_LOOKBACK = 5`는 너무 짧다.** 5일 전 대비는 노이즈에 가깝다. 20일 권장
   - 이격도 상한 (`DISPARITY_MAX`)
   - **이격도 하한 — 현재 누락.** 20일선을 크게 이탈한 종목(추세 훼손)이 통과할 수 있다

3. **되돌림**
   - **`range_pct` 하한 — 현재 누락.** 20일 고저폭이 5% 미만이면 횡보라 되돌림 개념이 무의미
   - **`days_since_high` 2~15일 ← INV-4, 현재 누락**
   - **`dd_from_high` 상한 — 현재 누락** (기본 18%)
   - **최근 3일 최저 종가면 제외 — 현재 누락.** 아직 떨어지는 중인 종목
   - `retrace_ratio` 통과 구간 (`RETRACE_MIN`~`RETRACE_MAX`, 기본 0.20~0.70)

---

## 4. 스코어링 & 출력

### 스코어링 freshness ✅ 2026-09-06 반영
기존 `freshness_n = clip(1 - (freshness-1)/(RETRACE_WINDOW-1), 0, 1)` 은
`freshness = 1`(고점 바로 다음날)에 **만점**을 줘서 방향이 반대였다.

현재는 `freshness_score()` 고원(plateau)형으로 교체:
이상 구간(`FRESHNESS_IDEAL_MIN`~`MAX`, 기본 3~8일) 만점, 그 밖으로 벗어날수록 감점.
`days_since_high = 1`은 INV-4 필터에서 먼저 탈락하므로 점수 계산에 도달하지 않는다(`score=None`).

| 경과일 | 1 | 2 | 3~8 | 10 | 12 | 15 |
|---|---|---|---|---|---|---|
| 이전 | 1.00 | 0.95 | 0.89~0.63 | 0.53 | 0.42 | 0.26 |
| 현재 | (필터 탈락) | 0.67 | 1.00 | 0.71 | 0.43 | 0.00 |

### 기타
- `advance_vol`을 `peak_i-20 : peak_i+1`(20일)로 잡고 있다. 되돌림 이전 구간 전체가
  섞여 상승 구간 거래량이 희석된다. **고점 직전 5일** 권장
- 유니버스가 작으므로 **상위 N개로 자르지 않는다.** 통과 종목 전부를 점수 내림차순으로

### 출력
- 날짜별 CSV 1개 (모든 지표 컬럼 포함, 밴드 탈락 종목의 `retrace_ratio`도 포함)
- 자체 완결형 HTML 1개 — **외부 CDN·폰트·스크립트 참조 0건**
  종목명, 코드, 종가, 시총, 되돌림비율, 각 이평선 대비 위치, 점수, 미니 차트

---

## 5. 관리종목 / 거래정지 / 투자경고

KRX가 안정적인 공개 API를 주지 않으므로 3단계로 처리한다.
어느 방법이 실제로 쓰였는지 로그로 남길 것.

1. 데이터 소스가 해당 정보를 주면 사용
2. `config/exclude_tickers.txt` — 사용자가 직접 관리하는 제외 목록
3. 데이터 휴리스틱 — 최근 10일 중 거래대금 0원인 날이 있으면 거래정지로 간주,
   최근 20일 중 ±25% 이상 단일 변동이 있으면 이상급등으로 간주

---

## 6. 데이터

- **수정주가 필수** (INV-6). 현재 미충족
- 초기 2~3년치를 로컬에 캐시하고, 이후 실행은 마지막 캐시 날짜 다음날부터만 증분 수집
- 거래대금은 실제 값을 쓴다. `종가 × 거래량` 근사를 쓰는 경우 그 사실을 컬럼명이나
  로그에 명시할 것 (한국 시장 거래대금과 오차가 있다)
- 데이터 수집이 실패하면 조용히 넘어가지 말고 **명확히 보고**한다
  (현재 `stale` 종목 체크가 있다. 좋다. 유지)

---

## 7. 검증 의무 (수정할 때마다 실행하고 결과를 보고)

`tests/` 아래 자동화하고, 수정할 때마다 전부 돌린다.

### V-1. 되돌림비율 독립 재계산
통과 종목 상위 8개에 대해 원시 종가 배열에서 H·L·되돌림비율을 손으로 다시 계산해
스크리너 출력값과 일치하는지 확인. 불일치가 하나라도 있으면 실패.

### V-2. 종가 전용 음성 테스트 (INV-1 회귀 방지)
장중 저가(`low`)는 완전히 동일하고 **종가 궤적만 다른** 두 가상 종목을 만든다.
- A: 종가가 고점 대비 얕게만 하락(되돌림 ≈ 0.10), 장중 저가는 20일선 크게 하회
- B: 종가가 실제로 되돌림(되돌림 ≈ 0.45), 장중 저가는 A와 동일

**기대: A 탈락, B 통과.** A가 통과하면 어딘가에서 `low`를 읽고 있다는 뜻이다.

### V-3. 고점 경과일 테스트 (INV-4 회귀 방지)
`days_since_high20 = 1`인 종목이 통과하는지 확인. **통과하면 실패.**

### V-4. 백테스트성 분포 확인
최근 120~130 거래일에 대해 날짜별 통과 종목수 분포를 낸다. 보고 항목:
평균 / 중앙값 / 최소 / 최대 / 사분위 / 0종목인 날의 비율 / 20종목 초과인 날의 비율 /
유니버스 대비 통과율.

**판정 기준**: 0종목인 날이 5%를 넘거나 20종목 초과인 날이 25%를 넘으면 파라미터가 틀렸다.
0종목인 날이 있으면 **그 날의 퍼널을 출력해 어느 단계에서 잘렸는지** 설명하라
(시장 전체 하락일이면 정상이다).

### V-5. 파라미터 민감도
`RETRACE_MIN`/`RETRACE_MAX`, `DISPARITY_MAX`를 몇 가지로 바꿔가며 통과 종목수 변화를 표로.
어떤 파라미터가 **실제로 아무것도 자르지 않고 있는지**(binding하지 않는지) 함께 보고할 것.
(경험상 되돌림 종목은 이미 20일선 근처라 `DISPARITY_MAX`가 거의 binding하지 않는다)

### V-6. 개별 종목 육안 검증
통과 종목 2~3개의 최근 15일 종가·MA20 대비 위치를 출력하고,
정말 "고점 → 눌림 → 20일선 부근 → 반등" 형태인지 수치로 확인.

### V-7. 제외 규칙 단위 테스트
실제 KRX 종목명으로 판별 함수를 테스트한다:
`삼성전자`(통과), `삼성전자우`(우선주), `현대차2우B`(우선주),
`메리츠금융지주`(**통과** — 리츠 아님), `ESR켄달스퀘어리츠`(리츠),
`디비금융제11호스팩`(스팩), `KODEX 200`(ETF)

---

## 8. 회귀 케이스 (실제로 잘못 잡혔던 사례)

### R-1. KB금융(105560), 기준일 2026-09-04
```
20일 고점 177,900 (2026-09-03) · 20일 저점 160,000
종가 ≈ 172,000 · retrace_ratio 0.33 · disparity_vs_ma20 1.02
days_since_high20 = 1 · 점수 69.7
```
**기대: 탈락** (INV-4 위반 — 고점이 전날).

오판 원인 두 가지:
1. `screen_on_date()`의 통과 조건이 `is_uptrend` + 거래대금 + 되돌림 밴드뿐이라
   `days_since_high20`이 필터에 전혀 반영되지 않았다
2. `freshness_n`이 `days_since_high = 1`에 **만점**을 줘서 점수까지 밀어올렸다

실제로는 상승 진행 중이었고, 거래량도 마르지 않고 오히려 급증 상태였다.
저점 160,000은 창 안쪽의 낡은 값이라 분모가 실제 되돌림 폭과 무관했다.

이 케이스를 테스트에 고정해두고, 수정할 때마다 여전히 탈락하는지 확인하라.

---

## 9. 위반 목록 (최종 갱신 2026-09-06)

우선순위 순. 고칠 때마다 이 목록을 갱신할 것.

### 처리 완료

| # | 항목 | 처리 | 검증 |
|---|---|---|---|
| 1 | INV-4 | `DAYS_SINCE_HIGH_MIN/MAX` 필터를 `screen_on_date()`·대시보드 양쪽에 추가. 탈락 종목은 `is_candidate=False` + `exclude_reason`으로 보존 | V-3a/b/c 통과. 기준일 2026-09-04 후보 5 → 2종목, R-1 탈락 |
| 2 | §4 | `freshness_score()` 고원형으로 교체. 경과일 필터 탈락 시 `score=None` | V-3d 통과 (1일 0.33 < 4일 1.00) |
| 3 | INV-6 | **전제가 사실이 아니었음.** 소스는 이미 분할·증자 조정됨(배당 미조정). 문구 정정 + `detect_price_discontinuity()` 상시 가드 추가 | 141종목×3년 ±30% 밖 불연속 0건, 야후 raw 대비 중앙값 1.0000 |
| 5-a | §3-1 | ETF/ETN/ELW 이름 제외 추가 (`ETF_BRAND_PREFIXES`). 접두어 뒤 공백/숫자를 확인해 ACE·SOL·PLUS 오탐 방지 | V-7 7/7 통과. **실제 유니버스에 섞여 있던 ETF 2종(`KODEX 머니마켓액티브`, `KODEX 200TR`) 제거 → 141 → 139종목** |

### 미처리

| # | 항목 | 심각도 | 내용 |
|---|---|---|---|
| 4 | §3-3 | 중간 | `dd_from_high`, `range_pct`, "최근 3일 최저 종가" 필터 부재 |
| 5-b | §3-1 / §5 | 중간 | 관리종목/거래정지/투자경고 처리 — 네이버 아이콘 best-effort만 있고 `config/exclude_tickers.txt`·데이터 휴리스틱 미구현. 우선주 종목코드 끝자리 판별도 미적용 |
| 6 | §3-2 | 중간 | `MA60_SLOPE_LOOKBACK = 5` 너무 짧음, 이격도 하한 없음 |
| 7 | §4 | 낮음 | `advance_vol` 20일 → 5일로 좁힐 것 |
| 8 | §3 | 낮음 | 퍼널 출력 — 실행 로그·V-4 0종목일 퍼널에는 반영됨. CSV/HTML 단계별 집계는 미완 |
| 9 | INV-1 | 낮음 | `high`/`low` 컬럼이 하류에 노출되어 있음 (동작은 정상) |

### 검증 실행 방법
```
python3 tests/test_screener.py     # V-3 / V-4 / V-7, 네트워크 불필요
```
